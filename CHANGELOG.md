# CHANGELOG

كل التغييرات المهمة على المشروع بتتسجل هنا.
الصيغة مبنية على [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/).

## [Unreleased]

### Added — الأسطول: مخالفات مرورية + ربط محاسبي تلقائي كامل

- سجل مخالفات مرورية (`fleet_violations`) بحالات (غير مسددة/مسددة/معترض
  عليها/مُلغاة)، مع تحميلها على الشركة أو على السائق.
- `FleetAccountingService` بيولّد القيد المحاسبي تلقائيًا لحظة تسجيل أي
  صيانة أو تعبئة وقود (مدين مصروف / دائن نقدية)، ولحظة سداد المخالفة
  (`POST /fleet/violations/{id}/pay`) — مصروف على الشركة أو ذمة على السائق.
- حسابات جديدة في شجرة الحسابات: صيانة المركبات (5410)، الوقود (5420)،
  المخالفات (5430)، ذمم السائقين (1180).
- شاشة "المخالفات المرورية" في الفرونت إند بزرار "تسجيل السداد".

### Added — نقطة بيع (Retail POS)

- موديول جديد بالكامل: شيفتات كاشير (فتح برصيد نقدي افتتاحي، قفل بتسوية
  الدرج وحساب الفرق تلقائيًا من مبيعات الكاش)، ومبيعات نقطة بيع.
- `PosSaleService`: عملية checkout ذرّية واحدة — تتأكد إن الشيفت مفتوح،
  تصرف المخزون فعليًا (وترفض لو الكمية غير كافية)، تولّد فاتورة حقيقية
  مدفوعة فورًا (بنفس نموذج e-invoicing)، وتولّد قيد محاسبي مُرحّل بمدين
  نقدية ودائن إيراد وضريبة، بالإضافة لقيد تكلفة البضاعة المباعة (أول
  استخدام فعلي لحسابي COGS/Inventory المعرّفين مسبقًا في `DefaultAccounts`).
- شاشتين في الفرونت إند: "شيفتات الكاشير" و"المبيعات" (ببناء سلة بيع
  متعددة الأصناف بنفس آلية `items` المستخدمة في طلبات البيع).

## [0.6.0] — تغطية كل الموديولات + النشر على استضافة مشتركة

### Added — Frontend: ٢٤ شاشة جديدة

الباك إند كان فيه ٣٦ كنترولر، والواجهة فيها ٨ شاشات بس — يعني معظم النظام
مكانش ليه واجهة. الشاشات اللي اتضافت:

- **محاسبة**: شجرة الحسابات، القيود اليومية (بمحرّر سطور وترحيل).
- **مبيعات**: العملاء، طلبات البيع (ببنود وتأكيد بيخصم من المخزون).
- **مشتريات**: الموردين، أوامر الشراء (ببنود).
- **مخزون**: المستودعات، المنتجات، حركات المخزون.
- **تصنيع**: قوائم المواد (BOM)، أوامر الإنتاج (بدء/إنهاء).
- **مشاريع**: المشاريع، العقود، العقارات، تفتيشات الجودة، الأصول الثابتة (تشغيل إهلاك).
- **موارد بشرية**: الموظفين، دورات الرواتب (ترحيل)، الوظائف الشاغرة، المتقدمين (تعيين).
- **CRM ودعم**: العملاء المحتملين (تحويل لعميل)، الفرص (نقل مرحلة)، تذاكر الدعم.
- **مالية**: الحسابات البنكية، الحركات البنكية (تسوية)، الموازنات.
- **نظام**: المستندات (رفع وتحميل)، المستخدمين.

**طبقة موارد مبنية على تعريف** بدل تكرار الكود: `components/ResourcePage.tsx`
و`api/resource.ts` بيغطوا نمط CRUD المشترك، وكل موديول بقى ملف تعريف في
`src/resources/`. إضافة موديول جديد محتاجة تعريف بس — من غير راوت ولا hooks ولا شاشة.

كمان: ترقيم صفحات، بحث، فلاتر، نوافذ منبثقة للنماذج، وقائمة جانبية مجمّعة
بتتفتح على الموبايل (الشاشات بقت كتير على قائمة ثابتة).

### Added — Backend

- **`UserController`** + راوتس `/v1/core/users`. حقول زي `project_manager_id`
  و`assigned_to` و`inspector_id` كانت في التحقق من غير endpoint يجيب أسماء
  المستخدمين — يعني مكانش ينفع تتعبّى من الواجهة أصلاً. الحذف محمي: مش ممكن
  تحذف حسابك ولا آخر حساب admin.
- **رفع ملفات حقيقي في المستندات.** كان بيسجّل `file_path` نصّي بس. دلوقتي
  `multipart` مع قايمة سماح للامتدادات، والتخزين في `storage/app/private`
  والتحميل عبر راوت محمي — **مش** عبر `storage:link`، لأن الـ symlink معطّل
  على أغلب الاستضافات المشتركة.
- `flat=1` في شجرة الحسابات — الافتراضي بيرجّع الحسابات الرئيسية بس والفرعية
  جوّاها، فقوائم اختيار الحساب كانت هتطلع ناقصة.
- SPA fallback في `routes/web.php` — لما الواجهة والـ API على دومين واحد،
  أي مسار مش API بيرجّع `index.html` عشان الروابط المباشرة تشتغل.
- `FORCE_HTTPS` و`DB_STRING_LENGTH` كمتغيرات بيئة.

### Added — النشر

- [`docs/deployment/INFINITYFREE.md`](docs/deployment/INFINITYFREE.md) — دليل كامل
  بالعربي، مع قسم صريح للقيود اللي هتقابلك.
- `deploy/infinityfree/` — `.htaccess` للجذر ولمجلد التطبيق، نقطة دخول معدّلة،
  قالب `.env`، و`setup.php` لتشغيل المايجريشن من المتصفح بتوكن سرّي (مفيش SSH).
- `deploy/infinityfree/build.sh` — بيبني الواجهة والباك إند ويجمّعهم في مجلد
  جاهز للرفع، وبيشيل الاختبارات والتوثيق من `vendor/` وبيطبع عدد الملفات
  (الحد المجاني 30,000 inode، و`vendor` لوحده بيقرب منه).
- `Makefile` — كان فاضي (صفر بايت). دلوقتي فيه أوامر التطوير والنشر.

### Fixed

- **اسم متغير الواجهة غلط في `render.yaml` والدليل.** كان `VITE_API_URL`
  والواجهة بتقرا `VITE_API_BASE_URL` — يعني أي نشر على Render كان هينتج واجهة
  بتنادي دومين نفسها وترجع 404 على كل حاجة.
- **`file_path` كان `varchar(255)` والتحقق بيسمح بـ 500 حرف.** أي مسار أطول من
  255 كان هيرمي خطأ من قاعدة البيانات بدل رسالة تحقق.
- **مقارنة توازن القيد بـ `!==` في `JournalEntryController::store`.** نفس فئة
  الباج اللي اتصلحت قبل كده في `isBalanced` — المقارنة الصارمة بين float ممكن
  ترفض قيد متوازن فعلاً. اتغيرت لمقارنة بهامش.
- **تحقّق الاستضافة المشتركة بـ JavaScript كان بيكسّر التطبيق بصمت.** كوكي
  `__test` بتنتهي بعد ~٦ ساعات، وبعدها الاستضافة بترجّع صفحة HTML مكان أي رد
  JSON. `lib/api.ts` بقى يكتشف الصفحة دي ويعيد تحميل الصفحة مرة واحدة عشان
  الكوكي تتجدد.
- **تعارض أسماء العلاقات مع المفاتيح الأجنبية.** Laravel بيسريلايز علاقة زي
  `assignedTo` تحت اسم `assigned_to`، فبتغطي على عمود المفتاح الأجنبي اللي بنفس
  الاسم. نماذج التعديل كانت هتقرا كائن بدل رقم.

### Notes

- الواجهة اتبنت فعليًا (`npm run build`) والبناء بينجح، وفحص الأنواع (`tsc`) نضيف.
- فحوصات `scripts/verify/` اتشغّلت ونجحت (١٠ فحوصات + ٨ فحوصات).
- اختبارات PHPUnit لسه **ما اتشغّلتش** — packagist محجوب في بيئة التطوير
  المستخدمة، فـ `composer install` مش ممكن. كل ملفات PHP اتفحصت بـ `php -l`.

## [Unreleased] — إصلاحات

### Fixed
- **جداول البنية التحتية الناقصة.** `.env.example` كان بيحدد `CACHE_STORE=database` و`QUEUE_CONNECTION=database` رغم إن جداول `cache` و`cache_locks` و`jobs` و`job_batches` و`failed_jobs` مش موجودة — يعني أول استدعاء للكاش أو أول job كان هيرمي "Table doesn't exist". اتضافوا في migration `000000` عشان تشتغل قبل أي حاجة تانية.
- **مقارنة أرقام عشرية بـ `===` في `JournalEntry::isBalanced()`.** المقارنة الصارمة بين float ممكن تفشل بسبب التمثيل الثنائي (27000.000000000004 ≠ 27000.0) وترفض قيد متوازن فعلاً. اتغيرت لمقارنة بهامش `< 0.01`. الدالة دي بتتحكم في ترحيل القيود اليدوية وقيد الإقفال السنوي.
- **CORS غير معرّف.** مفيش `config/cors.php`، فالواجهة على دومين مختلف عن الـ API كانت هتتحجب من المتصفح في الإنتاج. اتضاف الملف مع متغير `CORS_ALLOWED_ORIGINS`.
- **`php artisan test` كان بيفشل من أساسه.** `composer.json` بيشاور على `tests/` لكن المجلد و`phpunit.xml` مش موجودين. اتضافوا مع `tests/TestCase.php`.
- **تخصيص سندات الصرف مكنش ممكن من الواجهة.** شاشة السندات كانت بتعرض حقول التخصيص لسندات القبض بس، فسداد المورد مكنش ينفع يترّبط بفواتيره. دلوقتي النوعين مدعومين، والاستعلام مش بيتنفّذ غير لما الطرف يتحدد (`enabled`).

### Added
- `tests/Feature/AccountingPeriodGuardTest.php` — 8 اختبارات لحماية الفترات المقفلة.
- `tests/Feature/FinancialCycleTest.php` — 7 اختبارات للدورة المالية end-to-end وتوازن التقارير بعد كل خطوة.
- `scripts/verify/` — فحوصان مستقلان بيشتغلوا بـ SQLite من غير Laravel ولا composer، وبيرجعوا exit code فينفعوا في CI.

> **ملاحظة:** اختبارات PHPUnit مكتوبة لكن **ما اتشغّلتش** — packagist محجوب في بيئة التطوير المستخدمة، فـ `composer install` مش ممكن. فحوصات `scripts/verify/` اتشغّلت فعلاً ونجحت.

## [0.4.0] — إقفال الفترات + إكمال شاشات الواجهة

### Added — Backend: الفترات المحاسبية (`accounting/period-closing`)
- جدول `accounting_periods` + موديل `AccountingPeriod` بفترات شهرية لكل سنة مالية.
- `AccountingPeriodController`: عرض، توليد فترات السنة، إقفال، إعادة فتح (admin بس)، فحص حالة تاريخ، والإقفال السنوي.
- **`JournalEntryObserver`** — حماية مركزية مسجّلة على `JournalEntry` بتمنع إنشاء/تعديل/حذف أي قيد مُرحّل تاريخه في فترة مقفلة. بتغطي كل مسارات الترحيل دفعة واحدة (فواتير، سندات، رواتب، إهلاك، قيود يدوية)، وأي مسار جديد بيبقى محمي تلقائياً.
- الإقفال بيرفض لو لسه في قيود مسودة داخل الفترة — عشان ما تضيعش.
- **قيد الإقفال السنوي**: بيصفّر الإيرادات والمصروفات وينقل صافي النتيجة للأرباح المحتجزة (3200)، وبيرفض التكرار.
- `AccountingPeriod::withoutGuard()` — تعطيل مؤقت للحماية، مستخدم في قيد الإقفال السنوي بس لأن تاريخه ٣١ ديسمبر.
- `DefaultAccounts::retainedEarnings()` (3200) + إضافته لشجرة الحسابات الابتدائية.

### Added — Frontend
- **إنشاء فواتير المبيعات** من الواجهة: بنود متعددة، اختيار منتج بيملّي الاسم والسعر تلقائياً، بنود حرة، وحساب فوري للمجموع والضريبة والإجمالي.
- **شاشة الفترات المحاسبية**: توليد فترات السنة، إقفال/إعادة فتح كل فترة، وتشغيل الإقفال السنوي مع عرض صافي النتيجة.

### Notes
- منطق الإقفال السنوي وحدود حماية الفترات اتفحصوا بـ harness مستقل على SQLite (10 فحوصات): الإيرادات والمصروفات بتتصفّر، الأرباح المحتجزة = صافي الربح، معادلة المركز المالي تفضل متزنة بعد الإقفال، وحدود الفترات مضبوطة على أول/آخر يوم.
- الفرونت إند اتبنى فعلياً (`npm run build`) والبناء بينجح.

## [0.3.0] — أحدث الإصدارات + واجهة الإدارة

### Added — Frontend (`apps/frontend`)
- واجهة React كاملة كانت المجلد فاضي قبل كده: React 19.2 + Vite 8 + TypeScript 7 + Tailwind 4 + TanStack Query 5 + React Router 8 + Recharts 3.
- شاشات: تسجيل دخول، لوحة مؤشرات (KPIs + اتجاه مبيعات 12 شهر + أعلى العملاء)، فواتير المبيعات مع الإصدار، فواتير الموردين مع الاعتماد، سندات القبض والصرف مع التخصيص على الفواتير والإلغاء، والتقارير المالية (ميزان مراجعة / قائمة دخل / مركز مالي / أعمار الذمم).
- عربي كامل بـ RTL أصلي، مع عزل الأرقام والتواريخ بـ `dir="ltr"` عشان ما تتلخبطش داخل النص العربي.
- طبقة `api/hooks.ts` بتغلّف كل الـ endpoints، وأي عملية محاسبية بتعمل invalidate للفواتير والتقارير واللوحة مع بعض.
- تقسيم الحزمة بـ `lazy()` — الحزمة الأولية ~105 kB مضغوطة، وrecharts بيتحمّل مع اللوحة بس.

### Changed — Backend
- ترقية Laravel 11 → **13.17** (يتطلب PHP 8.3+)، وTinker → 3.x، وPHPUnit → 12.5، وPint → 1.27، وCollision → 8.6.
- `laravel/sanctum` اتثبّت على `^4.2` (تحققت إن 4.x بيدعم `illuminate/* ^13.0`).
- `nixpacks.toml`: `php82` → `php83` عشان النشر يوافق الحد الأدنى الجديد.
- `laravel/sail` اتشال من dev dependencies (`docker-compose.yml` موجود أصلاً في الجذر).

## [0.2.0] — إقفال الدورة المالية

### Added

**فواتير الموردين (`modules/finance` + `modules/procurement`)**
- جدولين `supplier_bills` و`supplier_bill_items` + موديلات `SupplierBill` / `SupplierBillItem`.
- `SupplierBillController` بـ CRUD كامل + `from-purchase-order/{po}` لتوليد الفاتورة من أمر شراء.
- `POST /approve` بيولّد قيد مُرحّل: مدين المصروف + ضريبة المدخلات (1170)، دائن الذمم الدائنة (2100).
- التعديل والحذف متاحين للحالة `draft` بس — الفاتورة المعتمدة ليها قيد في دفتر الأستاذ.

**سندات القبض والصرف (`modules/treasury`)**
- جدولين `payments` و`payment_allocations` (polymorphic) + موديلات `Payment` / `PaymentAllocation`.
- `POST /api/v1/finance/payments` بيعمل في transaction واحدة: السند + القيد المحاسبي + الحركة البنكية + تخصيص المبلغ على فواتير محددة + تحديث أرصدتها.
- تحقق كامل قبل الترحيل: إجمالي التخصيصات ≤ قيمة السند، الفاتورة تخص نفس الطرف، حالتها تسمح بالسداد، والمبلغ ≤ المتبقي عليها.
- `POST /{id}/void` بيلغي السند بقيد عكسي وحركة بنكية عكسية بدل حذف القيد الأصلي (حفاظاً على مسار التدقيق).

**التقارير المالية (`reports/financial`)**
- `App\Support\Reports\FinancialReports` — محرّك واحد بيحسب كل شيء من القيود المُرحّلة.
- `GET /api/v1/reports/`: `trial-balance`, `income-statement`, `balance-sheet`, `general-ledger/{account}`, `ar-aging`, `ap-aging`.
- ميزان المراجعة والمركز المالي بيرجعوا فحص `is_balanced` مع كل استجابة.
- كل التقارير بتقبل `project_id` — يعني قوائم مالية لكل مشروع على حدة.

**لوحة المؤشرات (`modules/dashboards`)**
- `GET /api/v1/dashboards/summary` — مؤشرات مالية وتشغيلية، اتجاه مبيعات 12 شهر، وأعلى 5 عملاء.
- المؤشرات المالية بتستدعي `FinancialReports` نفسه، فمفيش احتمال تضارب بين اللوحة والقوائم.

### Changed
- `Invoice`: عمود `paid_amount` جديد + `balance_due` و`payment_status` (`unpaid`/`partial`/`paid`) في كل استجابة، و`refreshPaidAmount()` بتقفل الفاتورة تلقائياً لما تتحصّل بالكامل.
- `DefaultAccounts`: إضافة الذمم الدائنة (2100)، ضريبة المدخلات (1170)، الصندوق (1110)، المخزون (1200)، تكلفة المبيعات (5100)، والمصروفات العمومية (5900).
- `ChartOfAccountsSeeder`: الحسابات الجديدة اتضافت لشجرة الحسابات الابتدائية.
- `Customer` و`Supplier`: علاقات `invoices` / `bills` / `payments` + `outstandingBalance()`.
- `PurchaseOrder`: علاقة `bills()`.
- middleware `role` اتطبق على اعتماد فواتير الموردين وإلغاء السندات.

### Notes
- تصميم القيود اتفحص بـ harness مستقل على SQLite: ميزان المراجعة متوازن، معادلة المركز المالي متزنة، وشرائح أعمار الديون مضبوطة على حدودها (0 / 30 / 60 / 90 يوم).
- حساب أيام التأخير بيستخدم فرق التواريخ المباشر بدل `diffInDays` عشان السلوك اتغير بين Carbon 2 و 3.

## [0.1.0]

### Added
- نواة Laravel API: auth (Sanctum)، شجرة حسابات، قيود يومية، مشاريع، عقود، عقارات، مشتريات، أصول ثابتة، موارد بشرية، مخزون، مبيعات، فوترة إلكترونية (ZATCA Phase 1)، تصنيع، CRM، رواتب، دعم فني، بنوك، موازنات، جودة، توظيف، وإدارة مستندات.
- الهيكل الكامل للمشروع (modules / core / ai / integrations / shared / reports / workflows / low-code / devops).
